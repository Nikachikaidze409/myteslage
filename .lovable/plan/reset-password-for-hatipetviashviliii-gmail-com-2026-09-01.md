# Reset password for hatipetviashviliii@gmail.com

## Why
Passwords are stored as one-way hashes, so the existing password cannot be read back. The only option is setting a new one.

## Change
- Set the password for the account `hatipetviashviliii@gmail.com` to `okayokay` using an admin-level backend update (no app code changes).
- Confirm the account exists and the update succeeds.

## After approval
Sign in at `/auth` with that email and the new password. This account already has the admin role, so `/map` opens without a paid membership.

## Notes
- No changes to app code, pricing, or payments.
- Recommend changing this password to something stronger later, since it was shared in chat.
